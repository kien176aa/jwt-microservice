server:
  port: 8082

spring:
  application:
    name: PRODUCT-SERVICE

eureka:
  client:
    service-url:
      defaultZone : ${EUREKA_DEFAULT_ZONE}  

url:
  identity:
    service: ${URL_IDENTITY_SERVICE} 